<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glacialware.r15k.view">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:fullBackupContent="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.splash.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.main.MainActivity"
            android:label="Main">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.test.TestActivity"
            android:label="Test" />
        <activity
            android:name=".views.addPlayer.AddPlayerActivity"
            android:label="@string/add_players" />
        <activity android:name=".views.cards.CardsActivity"
            android:label="@string/all_cards"/>
        <activity
            android:name=".views.cardDetail.CardDetailActivity"
            android:label="@string/title_activity_card_detail" />
        <activity
            android:name=".views.playerDetail.PlayerDetailActivity"
            android:label="@string/title_activity_player_detail"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="ce8cda0c517276b2d9240eb354c616722f238389" />
    </application>

</manifest>